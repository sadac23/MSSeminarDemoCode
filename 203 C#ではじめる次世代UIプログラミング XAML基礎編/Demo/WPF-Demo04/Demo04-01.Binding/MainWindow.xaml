<Window x:Class="Demo04_01.Binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <StackPanel x:Name="BindingPanel" Background="DarkBlue">
        <TextBox Text="{Binding Name}" FontSize="30" Margin="5" />
        <TextBox Text="{Binding Path=Age}" FontSize="30" Margin="5" />
        <TextBox Text="{Binding Address}" FontSize="30" Margin="5" />
    </StackPanel>
</Window>
